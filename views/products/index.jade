extends ../layouts/default

block content
  .row
    h1 Products
    .col-lg-8
      table.table.table-hover
        thead
          tr
            th Name
            th(style='width: 250px;') Barcode
        tbody
          for product in products
            tr
              td= product.title
              td= product.barcode
        tfoot
          form(action='/products/new', method='post')
            tr
              td
                input.form-control(type='text', name='title', placeholder='Product name')
              td
                .input-group
                  input.form-control(type='text', name='barcode', placeholder='Barcode')
                  span.input-group-btn
                    button.btn.btn-primary(type='submit') Add

    .col-lg-4
      .panel.panel-primary
        .panel-heading Metadata
        .panel-body.
          A number of <b>#{products.length}</b> products
